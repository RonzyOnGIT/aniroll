import { createSlice } from '@reduxjs/toolkit';

const animes = createSlice({
    name: 'animes',
    initialState: {
        genres: {

        },
        reducers: {}
    }
})

export default animes.reducer;
